spring:
  profiles:
    active: h2,log4jdbc
  jpa:
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true
    properties:
      hibernate:
        show_sql: false
        format_sql: false
